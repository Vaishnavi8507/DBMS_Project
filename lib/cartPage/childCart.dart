import 'package:flutter/material.dart';

class ChildCart extends StatelessWidget {
  const ChildCart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
