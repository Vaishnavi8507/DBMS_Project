import 'package:flutter/material.dart';

class EssentialCart extends StatelessWidget {
  const EssentialCart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
