import 'package:flutter/material.dart';

class SkinCart extends StatelessWidget {
  const SkinCart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
