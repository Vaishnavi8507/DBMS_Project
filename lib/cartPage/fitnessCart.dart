import 'package:flutter/material.dart';

class FitnessCart extends StatelessWidget {
  const FitnessCart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
