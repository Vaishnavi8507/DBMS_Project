import 'package:flutter/material.dart';

class AppointSummary extends StatelessWidget {
  const AppointSummary({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
